$kind: http-request
url: "{{baseUrl}}/api/v1/cart/items"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "sessionId": "{{sessionId}}",
      "variantId": "{{variantId}}",
      "quantity": 1
    }
order: 1000
