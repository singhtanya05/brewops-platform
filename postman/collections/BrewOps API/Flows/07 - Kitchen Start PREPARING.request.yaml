$kind: http-request
url: "{{baseUrl}}/api/v1/kitchen/orders/{{orderId}}/status"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "status": "PREPARING"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 7000
